import LinearGrandient from 'react-native-linear-gradient';
import styled from 'styled-components/native';

export default styled(LinearGrandient).attrs({
  colors: ['#700000', '#0A0808'],
})`
  flex: 1;
`;
